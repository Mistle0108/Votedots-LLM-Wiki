## 목적
- `Home`은 wiki의 진입 문서다.
- 이 폴더에서는 "어디부터 읽어야 하는가"를 가장 짧게 안내한다.

## 작성 규칙
- 상세 구현 설명은 다른 섹션으로 링크한다.
- 현재 상태와 다음 작업은 각각 `03-Status` 문서로 안내한다.
- 구조와 운영은 `02-Architecture` 문서로 안내한다.
- `Home`에는 중복 설명보다 읽는 순서와 핵심 링크를 우선 둔다.

