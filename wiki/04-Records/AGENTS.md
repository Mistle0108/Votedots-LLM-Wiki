## 목적
- `04-Records`는 프로젝트 작업/문제/결정 이력을 관리하는 섹션이다.

## 문서 역할
- `Worklog/`: 어떤 작업을 했는지
- `Issues/`: 어떤 문제가 있었는지
- `Decisions/`: 왜 그렇게 결정했는지

## 작성 규칙
- 현재 상태의 최신판은 여기에 두지 않고 `03-Status`에 반영한다.
- 구조 설명은 `02-Architecture`에 두고, 이 섹션에는 이력과 판단 근거를 남긴다.
- source fact는 `05-Sources`로 링크한다.

