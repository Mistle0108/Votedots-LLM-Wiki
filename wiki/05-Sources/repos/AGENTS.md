## 목적
- `repos/`는 repo 단위 source 요약을 관리하는 폴더다.

## 작성 규칙
- 대상 repo, branch, commit hash, 핵심 구조 사실을 frontmatter와 본문에 기록한다.
- overview 1개와 모듈 source 여러 개를 함께 둘 수 있다.
- 모듈 source는 LLM이 수정 위치를 판단할 수 있는 최소 기능 단위로 나눈다.
- 사용자별 실행 경로는 shared wiki에 적지 않는다.
- 구조/상태/우선순위 해석은 상위 문서에서 다루고 여기에는 사실만 둔다.
