# AGENTS.md

## 역할
- `raw/`는 불변 원본 또는 원본 참조 정보만 둔다.
- 원본 데이터는 수정하지 않는다.

## 규칙
- 초기 raw 범위는 `repo + issues + PR`다.
- repo는 읽기 전용으로 참조하고 ingest 기준 `commit hash`를 기록한다.
- 이슈/PR은 기본적으로 `링크 + 최소 메타데이터`만 둔다.
- 중요한 이슈/PR만 스냅샷을 추가한다.
- 정리, 해석, 판단, 우선순위는 `wiki/`에서만 한다.
